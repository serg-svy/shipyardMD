{
  "mcpServers": {
    "figma": {
      "command": "npx",
      "args": ["-y", "figma-developer-mcp", "--stdio"],
      "env": {
        "FIGMA_API_KEY": "${FIGMA_API_KEY}"
      },
      "description": "Figma MCP — reads design context, components, variables. Get your API key at figma.com/settings → Personal access tokens"
    }
  }
}
